# Parley data platform — Python dependencies.
# Python 3.10+ recommended. Install with: pip install -r requirements.txt

# --- ETL (extract) ---
pymongo==4.10.1              # MongoDB driver for the extract step
pyarrow==17.0.0             # Parquet read/write for the bronze/gold lakehouse layers
pandas==2.2.3              # used by the loader and the DuckDB fallback

# --- Big data (transform) ---
pyspark==3.5.3             # runs in local[*] mode out of the box — no cluster needed

# --- Warehouse (load) ---
snowflake-connector-python[pandas]==3.12.3   # write_pandas + COPY INTO loader
duckdb==1.1.3              # local warehouse fallback when Snowflake creds are absent

# --- Config / orchestration ---
python-dotenv==1.0.1       # load .env so it mirrors the Node app's config style
click==8.1.7               # pipeline.py CLI

# --- Optional: scheduling (only needed if you run the Airflow DAG) ---
# apache-airflow==2.10.3
